# v0.2 — The Journey Era

## Thesis

Praxis v0.2 transforms a curriculum into a world. v0.1 built measurement. v0.2 builds place.

---

## Taxonomy

| Term | Meaning | Scope |
|---|---|---|
| Challenge | Any individual learning activity | Universal |
| Stage | Pedagogical domain | Movement, Search, Editing, etc. |
| Family | Related activities within a stage | Stage-scoped, explicitly ordered |
| Layer | Learning purpose | Tutorial, Training, Trial, Boss |
| Region | UX identity of a stage | Frontend-only, presentation |
| Journey | User progression through curriculum | Computed by Journey Engine |
| Hub | Primary entrypoint | Neovim UI |

A challenge belongs to exactly one stage, exactly one family, and exactly one layer.

---

## Architecture Decisions

- **CLI owns truth. Neovim owns experience.** Go computes. Lua presents. Never inverted.
- **No standalone TUI.** The editor is the arena.
- **`plans/` documents intent.** Not UI mockups, not visual concepts, not narratives.
- **Existing tutorials are sacred.** No removal, rewrites, validator changes, or replay regressions across v0.2.x.

---

## Journey Model

| Step | Layer | Experience |
|---|---|---|
| Arrival | Onboarding | Welcome, orientation, first action, first success |
| Practice | Tutorial | Single-concept drills, highly guided |
| Synthesis | Training | Combined skills, family-based, composite evaluation |
| Proof | Trial | Multi-skill pressure, constrained execution |
| Application | Boss | Realistic tasks, meaningful application |

Onboarding is a frontend-only flow, not a layer. It is not curriculum, not persistent, not measured.

---

## Invariants

- 51/51 replay tests must pass throughout v0.2.x.
- The Hub always answers *what should I do next?*
- No infrastructure ships before its consumer.
- No feature may require accounts, servers, cloud state, or external services.
- Attempts, Completions, Success Rate, Mastery, and Confidence remain the authoritative measurement model.

---

## Release Arc

| Release | Question | Theme | Status |
|---|---|---|---|
| v0.2.0 | What kinds of experiences exist? | Curriculum Taxonomy | **Shipped** |
| v0.2.1 | Where do I begin? | Arrival | **Shipped** |
| v0.2.2 | Where am I? | Journey | **Shipped** |
| v0.2.3 | How do skills combine? | Training | **Shipped** |
| v0.2.4 | How do components relate canonically? | Canonicalization | |
| v0.2.5 | Can I prove competence? | Trials |
| v0.2.6 | Can I apply mastery? | Bosses |
| v0.2.7+ | What kind of place is this? | Identity |

Each release answers exactly one question. Infrastructure follows the consumer.

### v0.2.1 — Arrival (shipped)

**Question:** Where do I begin?

**Answer:** Welcome. Start here.

**Delivered:**
- Lua frontend extracted into focused modules (challenge.lua, session.lua, ui.lua, onboarding.lua, hub.lua)
- First-time onboarding flow: `:Praxis` → welcome → Enter → `motion_rush`
- Returning users experience v0.2.0 behavior unchanged
- 0 Go changes, 0 schema changes, 0 validator changes

**Deferred to v0.2.2:** Hub surface, Continue Journey, `praxis next`, region/stage language, progress displays, result screen navigation

### v0.2.3 — Training (shipped)

**Question:** How do skills combine?

**Answer:** Cross-family composition exercises combine concepts from different stages.

**Delivered:**
- 10 Training-layer challenges across Text Objects (5), Editing (2), and Registers (3)
- `composite` validator (byte-exact buffer comparison + MaxMoves constraint)
- Challenge `Evaluation` model with `MaxMoves` threshold
- `praxis describe <id>` — JSON description of any challenge
- `Re-validate` script for replay verification against `DescriptionFor`
- Documentation updated: PROGRESSION.md (Training section), CURRICULUM.md (Training table), REFERENCE.md, RELEASES.md

**Deferred:** Composite replay automation, Lua frontend Training surface

### v0.2.4 — Canonicalization (shipped)

**Question:** How do components relate canonically?

**Answer:** `DescriptionFor` is the single source of truth for all challenge data.

**Delivered:**
- `Describe` renamed to `DescriptionFor`, returns `(Description, bool)` struct
- `describe_test.go` rewritten: one subtest per field per challenge (`TestDescriptionForCompleteness`) + unknown ID test (`TestDescriptionForUnknown`)
- Old round-trip/evaluation tests removed (covered by completeness)
- `cmd/praxis/main.go`: `describe` uses `DescriptionFor` + `json.NewEncoder`; `record` validates IDs before saving
- `TestRecordUnknownChallenge` — record exits 1 for unknown IDs
- `scripts/generate_catalog.go`: consumes `DescriptionFor` struct, groups by Layer→Stage (not historical packs)
- CHALLENGES.md regenerated with Layer columns and Training section
- All docs audited (CURRICULUM.md, PROGRESSION.md, REFERENCE.md, VISION.md, TESTING.md, AUDIT.md, README.md)
- v0.1-style tests removed from describe_test.go (replaced by canonical completeness)

**Deferred:** Lua frontend canonical representation consumer

**Question:** Where am I?

**Answer:** Every challenge belongs to a stage of learning.

**Delivered (Phase 1):**
- `internal/content/curriculum.go` — production metadata source of truth (Metadata struct, 41-entry map, MetadataFor lookup, ValidStages)
- Taxonomy tests rewritten to validate against MetadataFor (coverage, valid stages, unique concept/context pairs, stage order, layer consistency)
- `praxis next` — returns next un-Practiced challenge ID in curriculum order

**Delivered (Phase 2):**
- `hub.lua` — Hub rendering with Location → Direction → Data layout
- `init.lua` — returning users dispatch to Hub instead of raw CLI output
- `challenge.lua` — result screen shows [Enter] Continue Journey and [q] Quit
- Enter on Hub or result screen opens next challenge directly (never Hub as intermediate)
- Documentation updated (ARCHITECTURE.md, REFERENCE.md)
- Zero Go changes, zero schema changes, zero validator changes

---

## Deferred Scope (v0.3+)

- Bronze → Grandmaster rank system
- Difficulty ratings
- Skill trees or dependencies
- Sequence validator
- Competitive features (leaderboards, achievements)
- Accounts, cloud sync, external services

---

## Open Questions

*None currently.*

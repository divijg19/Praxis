name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  certify:
    uses: ./.github/workflows/ci-reuse.yml
    with:
      certify: true

  release:
    needs: certify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v6
        with:
          go-version: stable

      - name: Build binary
        run: go build -o praxis ./cmd/praxis

      - name: Upload binary
        uses: actions/upload-artifact@v7
        with:
          name: praxis-${{ github.ref_name }}
          path: praxis
